#include <stdio.h>
/**
 * main - prints the program name with a newline slapped at the end
 * @argc: stores the argument count
 * @argv: the strings of the arguments
 * Return: 0 if true
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
