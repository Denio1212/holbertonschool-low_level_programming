#include "main.h"

/**
 * add - adds two integers and returns the result
 * @l: int to be added to r
 * @r: int to be added to l
 * Return: sum of l and r
 */
int add(int l, int r)
{
	int e;

	e = l + r;

	return (e);
}

