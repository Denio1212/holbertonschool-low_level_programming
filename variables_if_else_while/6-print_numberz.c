#include <stdio.h>

/**
 * main - prints all single digit numbers
 * starting from 0 with putchar
 * Return: Always 0 (Success)
 */


int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
	{
		putchar(b);

	}

	putchar('\n');

	return (0);
}
